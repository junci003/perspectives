# Perspectives

把一本书、一个人、或一套方法论的**思维方式**，蒸馏成能被 AI 直接调用的视角。

不是角色扮演，不是语录合集。提取的是**认知操作系统**——怎么想、怎么判断、什么不做、以及做不到什么。

## 什么是 perspective

一个 perspective 就是一个可加载的 Skill 目录：

```
[name]-perspective/
├── SKILL.md          每次加载都读：扮演规则 + 表达DNA + 加载路由
└── references/       按需展开：心智模型 / 决策启发式 / 失败清单 / 边界
```

平时只占几百 token，用到哪个模块才展开哪个文件——这样它能在你每次提问时自动挂载，而不会挤占对话空间。

## 已收录

| 视角 | 来源材料 | 内容 |
|---|---|---|
| [sunzi-perspective](sunzi-perspective/) | 《孙子兵法》十三篇原文 + 清华《珍藏版》 | 7 个心智模型（各含失效条件）、12 条决策启发式、3 份失败清单、5 组内在矛盾、46 案战例复核 |

## 安装

**方式一：ClawHub**

```bash
npm i -g clawhub
clawhub install sunzi-perspective
```

**方式二：手动**

```bash
git clone https://github.com/junci003/perspectives.git
cp -r perspectives/sunzi-perspective ~/.workbuddy/skills/
```

## 怎么用

不需要记命令，说到点子上就会自动挂载：

- 「用兵法看看这件事」「这仗怎么打」「要不要正面硬刚」
- 「切换到孙子视角」

想退出时说「切回正常」即可。

## 设计原则

1. **每个心智模型都写失效条件**，不能只写应用。只写应用的视角会让你到处用它，然后翻车。
2. **三重验证**：跨域复现 / 有生成力 / 有排他性。三条全过才算心智模型，只过一条降级为决策启发式。
3. **保留矛盾，不修复**。矛盾是人格特征，不是 Bug。
4. **诚实边界**。明确写出它答不了什么。一个不告诉你局限在哪的视角，不值得信任。
5. **不美化、不洗白**。如实呈现批评者视角与争议，不替原作者做价值观背书。

## 关于证据层级

以 `sunzi-perspective` 为例。材料分四层，**越往上越要打折扣**：

| 层 | 内容 | 可信度 |
|---|---|---|
| 文本层 | 十三篇原文 | 最高，可直接引用 |
| 训诂层 | 注释、异文、版本差异 | 需标注版本 |
| 验证层 | 战例 | **只可当提问，不可当证明**——全部是事后归因 |
| 转译层 | 现代案例 | 仅作素材；实测附会率极高 |

这个分层不是学术洁癖。它决定了一件实际的事：**当你用这个视角给自己的决策找依据时，哪一层的话可以信。**

## 同步

本机新增视角后，跑一次：

```powershell
.\sync.ps1
```

它会把 `~/.workbuddy/skills/*-perspective/` 下的所有视角目录同步到本仓库。

## 致谢

蒸馏方法论来自 **女娲 · Skill 造人术**（[花叔 Huashu](https://github.com/alchaincyf/nuwa-skill)，MIT 许可）。

## 版权说明

本仓库收录的是**框架性与方法性内容**（认知结构、判断规则、表达特征的提炼），可依 MIT 许可使用。

其中引用的第三方在版书籍内容（如清华大学出版社《孙子兵法（珍藏版）》的注释与解读）版权归原出版方所有，本仓库仅作学术性引用与评论，不构成对其内容的再授权。引用时请自行标注来源。
